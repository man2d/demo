<div class="content">

        <div class="left">
       		<%= render 'shared/breadcrumbs' %>
        	<h1><%= @item.title %><span><%= @item.lgth %></span></h1>
        </div>
        <div class="actions">
        	<p class="presence"><span class="color_gray_l">Наличие:</span> Есть на складе <span class="point">•</span></p>
        	<a href="#">Отправить заявку</a>
        	<a href="#" class="add">Добавить к сравнению</a>
        </div>
        
        <ul class="tabs">
        	<li class="current"><a href="#review" rel="review">Обзор</a></li>
            <% if @item.exterior_assets.size > 0 %><li><a href="javascript:void();" rel="exterior">Экстерьер</a></li><% end %>
            <% if @item.interior_assets.size > 0 %><li><a href="javascript:void();" rel="interior">Интерьер</a></li><% end %>
            <% if @item.specification_assets.size > 0 || @item.item_properties.size > 0 %><li><a href="javascript:void();" rel="specification">Спецификация</a></li><% end %>
            <% if @item.threed_assets.size > 0 %><li><a href="javascript:void();" rel="3dview">3-D вид</a></li><% end %>
            <li class="last"></li>
        </ul>

    <!--    <div class="b18-coverFlow" style="padding:0px">
	        <a class="left" href="#" id="ministers-left">
	            <img src="/img/b18/left.gif" alt="" />
	        </a>
	        <div class="leftzero"></div>
	        
	        <a class="right" href="#" id="ministers-right"><img src="/img/b18/right.gif" alt="" /></a>

	    </div>
	    
	-->
	<div id="tabs">
		
		<%= render 'catalog/review' %>
		<%= render 'catalog/threed_view' %>
        <%= render 'catalog/exterior' %>
		<%= render 'catalog/interior' %>
        <%= render 'catalog/specification' %>
        
		
    </div>
</div>