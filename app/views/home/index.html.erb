<div class="clear"></div>
<div class="main_banner">
	<div id="slider" class="images">
		<% Slide.all.each do |slide| %>
			<%= image_tag slide.image.url, :title => slide.title, :alt => '', :href => slide.item.url, :style => 'display:none;' %>
		<% end %>
	</div>

</div>

<div class="w910 index">
    	<div class="left">
        	<p class="about"><span></span><%= @page.body %>
            <h3>Яхты в <a href="/catalog">наличии</a></h3>
            <ul class="cols_3">
	        <% @items.each do |item| %>
            <li>
            	<a href="#"><img src="<%= item.image.url(:thumb) %>" alt="" /></a>
                <p><a href="<%= item.url %>"><%= item.title %></a><br />
                	Пробег: <%= item.mileage %> миль<br />
					Год выпуска: <%= item.year %></p>
            </li>
 			<% end %>
            </ul>
            <% if @page.pdf.url %><p class="file defaultPic"><a href="<%= @page.pdf.url %>">Каталог яхт</a>(5 Mб, ПДФ)</p><% end %>
        </div>
        <div class="right">
        	<div class="part">
        		<img src="images/ship.gif" alt="" /><a href="/about/sudovladeltsu">Судовладельцу</a>
            	<img src="images/cap.gif" alt="" /><a href="/about/kapitanu/">Капитану</a>
            </div>
            <h3><a href="/posts">Новости</a></h3>
            <div class="news">
				<% @posts.each do |post| %>
				  <%= link_to post.title, posts_url(post) %>
				<% end %>
            </div>
            <h3>Новое в <a href="/blog">блоге</a></h3>
            <div class="news">
			    <% @blog_posts.each do |post| %>
				  <%= link_to post.title, posts_url(post) %>
				<% end %>
            </div>
        </div>
        <div class="clear"></div>


</div>

<!--<div class="content">
    	<h1>Porsche Cayman S</h1>
<h2><a class="save">Сохранить</a></h2>
        <div class="news n_short">
        	<h2>Новости</h2>
<% if @posts %>
<% @posts.each do |post| %>
            <p><span class="date"><%= post.created_at.strftime("%d/%m/%Y") %></span><div class="editable" id="<%= dom_id(post)+"_announce" %>"><%= raw post.announce %></div></p>
<% end %>
<% end %>
        </div>
<% if @blocks %>
<% @blocks.each do |block| %>
<div class="editable"><%= raw block.content %></div>
<% end %>
<% end %>
        <div class="models mw620">
        	<h2>Модельный ряд Porsche</h2>
<% if @items %>
			  <% @items.each do |item| %>
        		<div class="cols_4"><center><%= image_tag item.image_url if item.image_url %><br /><a href="/catalog/show/<%= item.id %>"><%= item.title %></a><p><%= item.description %></p></center></div>
 		     <%= cycle("", "", "", raw('<div class="clear"></div>'))%>
              <% end %>
<% end %>            
            <div class="clear"></div>
        </div>
</div>
-->