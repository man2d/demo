<div class="formWrap">
<% form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => :registration}) do |f| %>
	<div class="w910"><h1>Регистрация</h1></div>
	<div class="blueBg">
		<div class="w910">
				<div class="radio">
					<label><%= f.radio_button :type, "captain" %>Капитан</label>
					<label><%= f.radio_button :type, "owner" %>Судовладелец</label>
				</div>
				<div class="text">
					<label for="name">Имя</label>
					<div class="left clear">
						<%= f.text_field :name, :value => "Имя" %>
						<div class="example">Пример: Имя</div>
					</div><div class="ready"></div><div class="info"></div>
				</div>
				<div class="text">
					<label for="mail">Электронная почта</label>
					<div class="left clear">
						<%= f.text_field :email, :value => "Электронная почта" %>
						<div class="example">Пример: почта@почта</div>
					</div><div class="not_ready"></div><div class="info">Адрес электронной почты будет логином для авторизации на сайте.
Адрес нигде не публикуется и не достанется спамерам.</div>
				</div>
				<div class="text">
					<label for="pass">Пароль</label>
					<div class="left clear">
						<%= f.password_field :password, :id => "user_password_password" %>
						<%= f.text_field :password, :id => "user_password_field", :style => 'display:none;'%>
						<div class="example"><a class="showPass" href="#">Показать пароль</a></div>
					  </div><div class="ready"></div><div class="info"></div>
				</div>
		</div>
	</div>
	<div class="w910">
		  <%= devise_error_messages! %>
		<div class="errorInfo">Введите корректный адрес электронной почты</div>
		<a class="submit" href="#" onclick="document.forms.user_new.submit;">Зарегистрироваться...</a>
	</div>
<% end %>
</div>
<!--
<h2>Sign up</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>


  <p><%= f.label :email %><br />
  <%= f.text_field :email %></p>

  <p><%= f.label :password %><br />
  <%= f.password_field :password %></p>

  <p><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></p>

  <p><%= f.submit "Sign up" %></p>
<% end %>

<%= render :partial => "devise/shared/links" %>
-->