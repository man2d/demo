<ul class="menu">
<% {:pages => "Страницы", :items => "Товары", :used_items => "Товары с пробегом", :posts => "Новости", :slides => "Слайдшоу", :hints => "Подсказки", :users => "Пользователи", :blogs => "Блоги"}.each do |controller, name| %>
    <% if params[:controller] == "admin/"+controller.to_s
	    klasss = "current"
       if params[:action] == 'index'
	     tag = :p
     else 
        klass = "" 
        tag = nil
       end
     end %> 

<%= content_tag(:li, :class => klasss) do
	if tag
		content_tag(:p, name)
	else
		link_to name, eval(("admin_"+controller.to_s+"_path"))
	end
end
%>
<% end %>
    
</ul>